node_modules
.next
.env.local
coverage
*.tsbuildinfo

# Planilhas internas do Senado (contêm PII) — nunca commitar.
# Fixtures de teste sob tests/fixtures/ são permitidas.
*.xlsx
!tests/fixtures/*.xlsx
